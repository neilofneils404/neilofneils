---
import { siteConfig } from '../data/site';
---
<footer class="site-footer" aria-label="Elsewhere online">
  <div class="footer-links">
    {siteConfig.socialLinks.map((link) => (
      <a href={link.href}>{link.label}</a>
    ))}
  </div>
</footer>
