---
import { headerLinks } from '../data/site';
---
<footer class="site-footer" aria-label="Elsewhere online">
  <div class="footer-links">
    {headerLinks.map((link) => (
      <a href={link.href}>{link.label}</a>
    ))}
  </div>
</footer>
